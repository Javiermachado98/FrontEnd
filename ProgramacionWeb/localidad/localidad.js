let enviar = document.getElementById('enviar')
enviar.addEventListener('click', arrancar)

function arrancar(){
    let pueblo = document.getElementById('localidad').value
    request = new XMLHttpRequest()
    request
}