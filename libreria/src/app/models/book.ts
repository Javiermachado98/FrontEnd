export interface Book {
    code: string;  // El código único del libro (ej: "L001")
    title: string; // El título del libro
    author: string;// El autor que lo escribió
    price: number; // El precio en euros
}
